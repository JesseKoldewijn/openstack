name: Benchmark Deep (scheduled)

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  PARITY_LOCALSTACK_IMAGE: localstack/localstack:3.7.2
  PARITY_OPENSTACK_IMAGE: ghcr.io/jessekoldewijn/openstack:latest
  PARITY_BENCHMARK_RUNTIME_MODE: symmetric-docker
  PARITY_BENCHMARK_EXECUTION_ORDER: alternating
  PARITY_DOCKER_CPU_LIMIT: "2"
  PARITY_DOCKER_MEMORY_LIMIT: 4g
  PARITY_DOCKER_NETWORK_MODE: bridge

jobs:
  benchmark-deep:
    name: Benchmark (hot-path-deep, non-blocking)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci-benchmark-deep
          key: benchmark-deep-${{ hashFiles('**/Cargo.lock') }}
      - name: Ensure AWS CLI is installed
        run: |
          set -euo pipefail
          if command -v aws >/dev/null 2>&1; then
            aws --version
            exit 0
          fi
          if command -v pipx >/dev/null 2>&1; then
            pipx install awscli || pipx upgrade awscli
            export PATH="$HOME/.local/bin:$PATH"
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          if ! command -v aws >/dev/null 2>&1; then
            curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip -q awscliv2.zip
            if command -v sudo >/dev/null 2>&1; then
              sudo ./aws/install --update
            else
              ./aws/install --install-dir "$HOME/.local/aws-cli" --bin-dir "$HOME/.local/bin" --update
              export PATH="$HOME/.local/bin:$PATH"
              echo "$HOME/.local/bin" >> "$GITHUB_PATH"
            fi
          fi
          aws --version
      - name: Preflight benchmark environment
        run: |
          set -euo pipefail
          command -v aws
          aws --version
          docker --version
          docker pull "$PARITY_OPENSTACK_IMAGE"
          docker pull "$PARITY_LOCALSTACK_IMAGE"
      - name: Run benchmark fairness high profile
        run: cargo run -p openstack-integration-tests --bin benchmark_runner -- --profile fair-high
      - name: Run benchmark fairness extreme profile (heavy objects, non-blocking)
        continue-on-error: true
        env:
          BENCHMARK_HEAVY_OBJECTS: "1"
        run: cargo run -p openstack-integration-tests --bin benchmark_runner -- --profile fair-extreme
      - name: Upload benchmark deep report artifacts
        if: always() && env.ACT != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-deep-report
          path: target/benchmark-reports/*.json
          retention-days: 14
