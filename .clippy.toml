# Global clippy configuration for openstack workspace
# Lint-level settings (deny/warn/allow) belong in Cargo.toml [workspace.lints]
# or in individual crate lib.rs/main.rs files.

cognitive-complexity-threshold = 30
too-many-arguments-threshold = 8
type-complexity-threshold = 500
