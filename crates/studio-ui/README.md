# openstack-studio-ui

Foundation crate for the Studio SPA client logic.

## Build

```bash
cargo test -p openstack-studio-ui
```

## Notes

- This crate currently contains typed API client/state/model foundations used by
  the future Leptos SPA runtime.
- Tailwind v4 and full SPA shell wiring are tracked in follow-up tasks.
